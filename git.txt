1.git init
2. git remote add origin <git-url>
3. git remote -v (to check)
 git status// compares online repo and local repo
4. git add <file_name>
U- untracked files
A- added// when we do git add <file_name>

5. git commit -m "index"
